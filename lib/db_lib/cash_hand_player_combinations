	cash_hand_player_combinations
id_hand (целое) - Идентификатор розыгрыша (1-∞).
id_player (целое) - Идентификатор ника игрока (1-∞).
flg_f_highcard (булева) - Если на флопе у игрока комбинация «старшая карта», значенье - True (истина), иначе - False (ложь).
flg_f_1pair (булева) - Если на флопе у игрока комбинация «одна пара», значенье - True (истина), иначе - False (ложь).
flg_f_2pair (булева) - Если на флопе у игрока комбинация «две пары», значенье - True (истина), иначе - False (ложь).
flg_f_threeoak (булева) - Если на флопе у игрока комбинация «трипс» или «сет», значенье - True (истина), иначе - False (ложь).
flg_f_straight (булева) - Если на флопе у игрока комбинация «стрит», значенье - True (истина), иначе - False (ложь).
flg_f_flush (булева) - Если на флопе у игрока комбинация «флеш», значенье - True (истина), иначе - False (ложь).
flg_f_fullhouse (булева) - Если на флопе у игрока комбинация «фулл-хаус», значенье - True (истина), иначе - False (ложь).
flg_f_fouroak (булева) - Если на флопе у игрока комбинация «каре», значенье - True (истина), иначе - False (ложь).
flg_f_strflush (булева) - Если на флопе у игрока комбинация «стрит-флеш», значенье - True (истина), иначе - False (ложь).
	id_f_hand_strength (м. целое) - Идентификатор силы комбинации игрока на флопе (0 – 10, для каждого типа комбинации идентификаторы имеют свои значения).
	id_f_kicker_strength (м. целое) - Идентификатор кикер карты игрока на флопе (0 – нет кикера,1 - 11: 1 – топ кикер, 11 – самый слабый кикер).
	val_f_hole_cards_used (м. целое) - Количество карманных карт игрока участвующих в составлении комбинации на флопе (0, 1, 2).
flg_t_highcard (булева) - Если на торне у игрока комбинация «старшая карта», значенье - True (истина), иначе - False (ложь).
flg_t_1pair (булева) - Если на торне у игрока комбинация «одна пара», значенье - True (истина), иначе - False (ложь).
flg_t_2pair (булева) - Если на торне у игрока комбинация «две пары», значенье - True (истина), иначе - False (ложь).
flg_t_threeoak (булева) - Если на торне у игрока комбинация «трипс» или «сет», значенье - True (истина), иначе - False (ложь).
flg_t_straight (булева) - Если на торне у игрока комбинация «стрит», значенье - True (истина), иначе - False (ложь).
flg_t_flush (булева) - Если на торне у игрока комбинация «флеш», значенье - True (истина), иначе - False (ложь).
flg_t_fullhouse (булева) - Если на торне у игрока комбинация «фулл-хаус», значенье - True (истина), иначе - False (ложь).
flg_t_fouroak (булева) - Если на торне у игрока комбинация «каре», значенье - True (истина), иначе - False (ложь).
flg_t_strflush (булева) - Если на торне у игрока комбинация «стрит-флеш», значенье - True (истина), иначе - False (ложь).
	id_t_hand_strength (м. целое) - Идентификатор силы комбинации игрока на торне (0 – 10, для каждого типа комбинации идентификаторы имеют свои значения).
	id_t_kicker_strength (м. целое) - Идентификатор кикер карты игрока на торне (0 – нет кикера,1 - 10: 1 – топ кикер, 10 – самый слабый кикер).
	val_t_hole_cards_used (м. целое) - Количество карманных карт игрока участвующих в составлении комбинации на торне (0, 1, 2).
flg_r_highcard (булева) - Если на ривере у игрока комбинация «старшая карта», значенье - True (истина), иначе - False (ложь).
flg_r_1pair (булева) - Если на ривере у игрока комбинация «одна пара», значенье - True (истина), иначе - False (ложь).
flg_r_2pair (булева) - Если на ривере у игрока комбинация «две пары», значенье - True (истина), иначе - False (ложь).
flg_r_threeoak (булева) - Если на ривере у игрока комбинация «трипс» или «сет», значенье - True (истина), иначе - False (ложь).
flg_r_straight (булева) - Если на ривере у игрока комбинация «стрит», значенье - True (истина), иначе - False (ложь).
flg_r_flush (булева) - Если на ривере у игрока комбинация «флеш», значенье - True (истина), иначе - False (ложь).
flg_r_fullhouse (булева) - Если на ривере у игрока комбинация «фулл-хаус», значенье - True (истина), иначе - False (ложь).
flg_r_fouroak (булева) - Если на ривере у игрока комбинация «каре», значенье - True (истина), иначе - False (ложь).
flg_r_strflush (булева) - Если на ривере у игрока комбинация «стрит-флеш», значенье - True (истина), иначе - False (ложь).
	id_r_hand_strength (м. целое) - Идентификатор силы комбинации игрока на ривере (0 – 10, для каждого типа комбинации идентификаторы имеют свои значения).
	id_r_kicker_strength (м. целое) - Идентификатор кикер карты игрока на ривере (0 – нет кикера,1 - 10: 1 – топ кикер, 10 – самый слабый кикер).
	val_r_hole_cards_used (м. целое) - Количество карманных карт игрока участвующих в составлении комбинации на ривере (0, 1, 2).
flg_r_low_hand (булева) - Если на ривере у игрока есть лоу комбинация (омаха хай-лоу), значенье - True (истина), иначе - False (ложь)
